"use strict";

var _child_process = require("child_process");

var argv = process.argv.slice(2);

var binIndex = argv.indexOf("-b");
var bin = "node";

if (binIndex > -1) {
    bin = argv[binIndex + 1];
    argv.splice(binIndex, 2);
}

argv.forEach(function (file) {
    var ps = (0, _child_process.spawn)(bin, [file], { stdio: "inherit" });
    ps.on("close", function (code) {
        return process.exit(code);
    });
});